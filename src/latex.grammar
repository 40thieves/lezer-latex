@top[name=LaTeX] { expression }

expression { text | Command | Math }

Command { "\\" text Options? Arguments? }

Options { "[" text? "]" }

Arguments { "{" text? "}" }

Math { "$" (text | Command)? "$" }

@tokens {
  text { std.asciiLetter+ }
}
